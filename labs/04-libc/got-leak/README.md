# got-leak

Objective: parse a code pointer, identify its library, calculate a page-aligned base, and decide what second-stage primitive is still needed. The fixture prints a pointer for study; the next exercise is to combine the calculation with the stack-control pattern.

