# Writeup: chunk-inspect

This fixture exposes allocator addresses for study. The useful evidence is allocation order, distance, and reuse behavior. A reliable heap analysis records glibc version, requested size, actual chunk size, and bin transition rather than copying a diagram.

