# Capstones

The capstone directories are intentionally separate from the teaching labs. Build the supplied challenge, create `exploit.py` from the starter, and complete `WRITEUP.md` using the repository template. A reference chain is included only after the learner contract is established.

