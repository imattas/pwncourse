# Stage 02 — stack control

The stack labs turn a memory-safety bug into an explicit control-flow primitive. First redirect execution to a nearby function, then satisfy the x86-64 calling convention, then handle an input path that reads in two stages. Every payload should be explained as an offset plus a sequence of 8-byte words.

